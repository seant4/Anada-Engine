#pragma once
#include "../Object.h"

class [Room(s) where this is implemented];

class [ObjectName]: public Object{
    public:
        //If our object has a texture
        SDL_Texture* texture;

        //If our object is animated
		int animationSpeed;
		SDL_Rect spriteClips[[Frames in spritesheet]];
        
        //Example game logic: Jumping state
		bool airborne;
        [ObjectName](int xi, int yi, int wi, int hi, bool ci, std::string namei, SDL_Renderer* r);
        ~[ObjectName]();

        //Overloading update to take input
        void update(int x, int y);
		void draw(SDL_Renderer* r) override;
};
